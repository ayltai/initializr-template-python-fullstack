# <%= name %> frontend

This is the frontend for the <%= name %> project. It is a React/TypeScript application.

## Development

To install the dependencies, run:

```bash
pnpm i
```

To start the frontend server, run:

```bash
pnpm start
```

The frontend server will be available at `http://localhost:5173`.

To build the frontend, run:

```bash
pnpm build
```

To lint the code, run:

```bash
pnpm lint
```

To run the tests, run:

```bash
pnpm test
```

To list all outdated dependencies, run:

```bash
pnpm outdated
```

To start the Storybook server, run:

```bash
pnpm storybook
```

The Storybook server will be available at `http://localhost:6006`.