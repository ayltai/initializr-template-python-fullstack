import { theme, type ThemeConfig, } from 'antd';

export const THEME : ThemeConfig = {
    algorithm : theme.darkAlgorithm,
    token     : {
        colorPrimary  : '#fa8c16',
        colorBgLayout : '#121212',
    },
};

export const PAGE_WIDTH : number = 1200;
