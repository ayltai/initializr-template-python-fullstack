# <%= name %> backend

This is the backend for the <%= name %> project. It is a Python/FastAPI application.

## Development

To create a virtual environment and install the dependencies, run:

```bash
make venv
source venv/bin/activate
make upgrade
```

To start the backend server, run:

```bash
make
```

The backend server will be available at `http://localhost:8000`.

To run the backend server in production mode, run:

```bash
make prod
```

To lint the code, run:

```bash
make lint
```

To list all outdated dependencies, run:

```bash
make outdated
```

To update the dependencies, run:

```bash
make upgrade
```
